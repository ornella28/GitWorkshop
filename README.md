# Exercise1
**git init**: initiate the ceated repository Exercise1
**git status**: check if the file created locally is showing in git##
**git add note.txt**: stage the created file 
**git add note.txt**. stage the modified text in the file note.txt
**git commit -m "modify the hello everyone to hello all!"**: commit the changes made to the text so that they can be saved properly
##changed the content of the file and wrote hello class
**git restore note.txt**: restored the content of note.txt to what it was previously
**git log**: see all the changes that have been made, from the creation to the file to the modified text
**git add . : stage all the changes
**git commit -m "modify the hello class to hello all"**: commit the new changes, transfer them to git
**git add .gitignore**: staged a file .gitignore i created
**git commit -m "added a gitignore file"**: transfer the gitignore file to git


